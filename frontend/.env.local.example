# ============================================
# Todo App Frontend - Environment Variables
# ============================================
# Copy this file to .env.local and update with your values
# NEVER commit .env.local to version control!

# ============================================
# Backend API Configuration (REQUIRED)
# ============================================
# The URL of your FastAPI backend server
# Development: http://localhost:8000
# Production: https://your-api-domain.com
#
# This variable is prefixed with NEXT_PUBLIC_ so it's available in the browser
# All API requests will be sent to this URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================
# Better Auth Configuration (REQUIRED)
# ============================================
# Secret key used for JWT token signing and verification
# MUST match the secret configured in your backend
#
# SECURITY REQUIREMENTS:
# - Use a strong, randomly generated secret (minimum 32 characters)
# - Never use the example value in production
# - Rotate secrets regularly (every 90 days recommended)
# - Keep this secret secure and never commit to version control
#
# Generate a secure secret:
# - Node.js: require('crypto').randomBytes(32).toString('hex')
# - OpenSSL: openssl rand -hex 32
# - Online: https://generate-secret.vercel.app/32
BETTER_AUTH_SECRET=your-secret-key-here-change-in-production

# The URL where your frontend application is hosted
# Development: http://localhost:3000
# Production: https://your-frontend-domain.com
#
# Used by Better Auth for:
# - Redirect URLs after authentication
# - CORS configuration
# - Cookie domain settings
BETTER_AUTH_URL=http://localhost:3000

# ============================================
# Environment (OPTIONAL)
# ============================================
# Application environment mode
# Values: development | production | test
#
# Effects:
# - development: Shows detailed error messages, enables debug logging
# - production: Optimized builds, minimal logging, error tracking
# - test: Used for automated testing
#
# Note: Next.js automatically sets this during build/dev
NODE_ENV=development

# ============================================
# Additional Configuration (OPTIONAL)
# ============================================

# Enable debug logging for API calls
# Values: true | false
# Default: false
# DEBUG_API=false

# API request timeout in milliseconds
# Default: 30000 (30 seconds)
# API_TIMEOUT=30000

# Enable analytics tracking
# Values: true | false
# Default: false
# NEXT_PUBLIC_ENABLE_ANALYTICS=false

# Google Analytics Measurement ID (if analytics enabled)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry DSN for error tracking (production)
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# ============================================
# Documentation & Resources
# ============================================
#
# Environment Variables Guide:
# - Next.js Docs: https://nextjs.org/docs/app/building-your-application/configuring/environment-variables
# - Better Auth Docs: https://better-auth.com/docs/configuration
#
# Security Best Practices:
# - Never commit .env.local or .env.production to version control
# - Use different secrets for development and production
# - Store production secrets in your deployment platform's secret manager
# - Rotate secrets regularly
# - Use HTTPS in production
#
# Troubleshooting:
# - If API calls fail, verify NEXT_PUBLIC_API_URL is correct and backend is running
# - If authentication fails, ensure BETTER_AUTH_SECRET matches backend configuration
# - Restart dev server after changing environment variables
# - Public variables (NEXT_PUBLIC_*) are embedded in the browser bundle
#
# ============================================
